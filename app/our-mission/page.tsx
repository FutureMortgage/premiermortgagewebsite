import type { Metadata } from "next";
import { AboutPage } from "@/components/ServicePages";

export const metadata: Metadata = { title: "Our Mission — Premier Mortgage Resources" };
export default function Page() { return <AboutPage />; }
