import type { Metadata } from "next";
import { PaymentPage } from "@/components/ServicePages";

export const metadata: Metadata = { title: "Make A Payment — Premier Mortgage Resources" };
export default function Page() { return <PaymentPage />; }
