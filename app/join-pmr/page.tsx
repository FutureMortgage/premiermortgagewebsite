import type { Metadata } from "next";
import { JoinPage } from "@/components/ServicePages";

export const metadata: Metadata = { title: "Join Pmr — Premier Mortgage Resources" };
export default function Page() { return <JoinPage />; }
