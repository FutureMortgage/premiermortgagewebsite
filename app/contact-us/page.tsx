import type { Metadata } from "next";
import { ContactPage } from "@/components/ServicePages";

export const metadata: Metadata = { title: "Contact Us — Premier Mortgage Resources" };
export default function Page() { return <ContactPage />; }
